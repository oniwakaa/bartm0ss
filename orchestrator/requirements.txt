# Core dependencies
ollama>=0.6.0
tree-sitter>=0.25.0
tree-sitter-python>=0.25.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.15.0

# Code quality
black>=24.0.0
ruff>=0.1.0
mypy>=1.8.0

# Optional: LSP support (installed separately)
# pyright - install via: npm install -g pyright
# typescript-language-server - install via: npm install -g typescript-language-server